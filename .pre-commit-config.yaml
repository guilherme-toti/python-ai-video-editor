repos:
  - repo: https://github.com/psf/black
    rev: 25.1.0 # Updated to match local version
    hooks:
      - id: black
        language_version: python3
        args: ["--config", "pyproject.toml", "--verbose"]
